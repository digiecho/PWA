let admin,createThumbnail;_155‍.x([["default",()=>_155‍anonymous]]);_155‍.w("firebase-admin",[["default",["admin"],function(v){admin=v}]]);_155‍.w("firebase-function-tools/lib/thumbnail",[["default",["createThumbnail"],function(v){createThumbnail=v}]]);


               async function _155‍anonymous(object) {
  const thumbnail = await createThumbnail(object)

  if (thumbnail) {
    const { fileDir, downloadURL } = thumbnail

    await admin
      .database()
      .ref(fileDir)
      .update({ thumbnail: downloadURL })
  }

  return
}
