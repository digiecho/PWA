let functions,admin;_155‍.x([["default",()=>_155‍.o]]);_155‍.w("firebase-functions",[["*",null,function(v){functions=v}]]);_155‍.w("firebase-admin",[["default",["admin"],function(v){admin=v}]]);


_155‍.d(functions.https.onCall(async (data, context) => {
  const { auth } = context
  if (!auth) {
    throw new functions.https.HttpsError(
      'failed-precondition',
      'The function must be called ' + 'while authenticated.'
    )
  }

  const { uid } = auth

  _155‍.g.console.log('uid', uid)
  _155‍.g.console.log('auth', auth)

  try {
    await admin.database().ref(`admins/${uid}`).set(true)
    return { message: 'OK' }
  } catch (error) {
    return error
  }
}));
